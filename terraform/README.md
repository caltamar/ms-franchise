# Terraform Infrastructure

This folder contains the Infrastructure as Code (IaC) definition for the project.

## Resources

- AWS Provider
- ECR Repository

## Commands

Initialize Terraform:

```bash
terraform init
```

Validate configuration:

```bash
terraform validate
```

Generate execution plan:

```bash
terraform plan
```

Apply infrastructure:

```bash
terraform apply
```